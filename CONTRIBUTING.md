# Contributing to LibreSlicer

1. Fork the repository and create a feature branch off `main`.
2. Keep pull requests focused; ensure CI is green and add a changelog note when appropriate.
3. At least one approving review is required before merges to `main`.

Release work should go through release branches and follow `.github/PULL_REQUEST_TEMPLATE/internal_release.md`.
